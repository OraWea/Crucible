# ============================================================================
# Crucible - 个人知识库系统配置
# ============================================================================
#
# 支持多种 LLM Provider 配置
#
# ============================================================================

# ----------------------------------------------------------------------------
# 基础路径配置（无需修改）
# ----------------------------------------------------------------------------
# Obsidian Vault 本地绝对路径
OBSIDIAN_VAULT_PATH=D:\Obsidian\MyVault

# 数据库路径
DATABASE_PATH=./data/crucible.db

# ----------------------------------------------------------------------------
# Whisper 语音识别模型配置
# ----------------------------------------------------------------------------
# 可选: tiny, base, small, medium, large-v3
WHISPER_MODEL_NAME=base
# 可选: cuda (需要NVIDIA显卡), cpu
WHISPER_DEVICE=cuda

# ----------------------------------------------------------------------------
# VLM 视觉模型配置（本地运行）
# ----------------------------------------------------------------------------
VLM_MODEL_ID=Qwen/Qwen2-VL-7B-Instruct
VLM_DEVICE=cuda

# ============================================================================
# LLM Provider 配置
# ============================================================================

# ----------------------------------------------------------------------------
# 选项1: 阿里云 DashScope (百炼) - 默认 Provider
# ----------------------------------------------------------------------------
LLM_API_KEY=your-dashscope-api-key-here
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_NAME=qwen-plus
LLM_TIMEOUT=60
LLM_MAX_TOKENS=2048

# ----------------------------------------------------------------------------
# 选项2: OpenAI
# ----------------------------------------------------------------------------
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_MODEL_NAME=gpt-4o

# ----------------------------------------------------------------------------
# 选项3: Anthropic Claude
# ----------------------------------------------------------------------------
# CLAUDE_API_KEY=your-anthropic-api-key-here
# CLAUDE_API_BASE=https://api.anthropic.com
# CLAUDE_MODEL_NAME=claude-3-5-sonnet-20241022

# ----------------------------------------------------------------------------
# 选项4: 本地模型 (Ollama)
# ----------------------------------------------------------------------------
# OLLAMA_API_BASE=http://localhost:11434
# OLLAMA_MODEL_NAME=llama3

# ----------------------------------------------------------------------------
# 选项5: LM Studio
# ----------------------------------------------------------------------------
# LMSTUDIO_API_BASE=http://localhost:1234/v1
# LMSTUDIO_MODEL_NAME=local-model

# ============================================================================
# 模块级模型配置（可选）
# ============================================================================
# 为不同模块指定不同的模型

# 概念提取模块 - 使用 GPT-4o
# CONCEPT_EXTRACTOR_PROVIDER=openai
# CONCEPT_EXTRACTOR_MODEL=gpt-4o

# 事实检查模块 - 使用便宜的模型
# FACT_CHECKER_PROVIDER=openai
# FACT_CHECKER_MODEL=gpt-3.5-turbo

# 维基合并模块 - 使用 Claude
# WIKI_MERGER_PROVIDER=claude
# WIKI_MERGER_MODEL=claude-3-5-sonnet-20241022
