# Issue: 多模型Provider支持与模块级配置

## 问题描述

### 痛点
- 现有项目只能使用阿里百炼(DashScope) API，缺乏灵活性
- 所有模块共用同一个模型，无法针对不同任务选择最优模型
- 难以扩展支持其他主流LLM服务

### 理想解决方案
- 支持更多常见模型API（OpenAI、Claude、本地模型等）
- 针对不同模块使用不同模型（这才是正常的逻辑）
- 保持向后兼容性，现有配置无需修改

---

## 解决方案

### 架构设计

采用**分层多Provider架构**：

```
Config层（环境变量配置）
    ↓
Provider层（API适配器）
    ├── DashScopeProvider（阿里百炼）- 向后兼容
    ├── OpenAIProvider（GPT-4系列）
    ├── ClaudeProvider（Claude 3系列）
    └── LocalProvider（Ollama/LM Studio）
    ↓
LLMManager（统一调度器）
    ↓
模块调用层（LLMCore、FactChecker等）
```

### 核心组件

#### 1. Provider抽象层
- `BaseLLMProvider`: Provider基类，定义统一接口
- `ProviderConfig`: 配置数据类
- `ProviderType`: Provider类型枚举

#### 2. Provider实现
| Provider | 支持模型 | 特点 |
|----------|---------|------|
| DashScopeProvider | qwen-plus, qwen-vl-plus | 向后兼容阿里百炼 |
| OpenAIProvider | gpt-4o, gpt-4-turbo, gpt-3.5-turbo | OpenAI官方API |
| ClaudeProvider | claude-3-5-sonnet, claude-3-opus | Anthropic Claude系列 |
| LocalProvider | llama3, qwen2, llava | Ollama/LM Studio本地部署 |

#### 3. LLMManager调度器
- 统一管理多个Provider
- 支持模块级Provider绑定
- 提供chat/vision_chat统一接口

---

## 实施细节

### 文件结构

```
models/
├── providers/
│   ├── __init__.py          # Provider模块导出
│   ├── base.py              # 基类和配置结构
│   ├── dashscope.py         # 阿里百炼实现
│   ├── openai.py            # OpenAI实现
│   ├── claude.py            # Claude实现
│   └── local.py             # 本地模型实现
├── llm_manager.py           # 统一调度器
├── llm_core.py              # 已重构：概念提取模块
├── fact_checker.py          # 已重构：事实检查模块
└── vlm_analyzer.py          # 视觉分析模块
```

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `models/providers/base.py` | Provider基类、配置类、注册表 |
| `models/providers/dashscope.py` | 阿里百炼API适配器 |
| `models/providers/openai.py` | OpenAI API适配器 |
| `models/providers/claude.py` | Claude API适配器 |
| `models/providers/local.py` | 本地模型适配器（Ollama/LM Studio） |
| `models/llm_manager.py` | LLM统一调度器 |
| `.env.example` | 新的环境变量配置模板 |
| `config_providers_example.py` | 多Provider配置示例 |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `models/llm_core.py` | 重构为使用LLMManager |
| `models/fact_checker.py` | 重构为使用LLMManager |
| `models/__init__.py` | 添加Provider模块导出 |
| `config.py` | 扩展配置项支持多Provider |

---

## 使用方法

### 1. 简单配置（保持原有用法）

```bash
# .env 文件 - 无需修改原有配置
LLM_API_KEY=your-dashscope-api-key
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_NAME=qwen-plus
```

**向后兼容**：原有代码完全可用，无需任何修改。

### 2. 多Provider高级配置

```python
from Crucible.models.llm_manager import llm_manager
from Crucible.models.providers import ProviderType, ProviderConfig

# 注册多个Provider
llm_manager.register_provider("openai-fast", ProviderConfig(
    provider_type=ProviderType.OPENAI,
    api_key="your-openai-api-key",
    api_base="https://api.openai.com/v1",
    model_name="gpt-4o-mini"
))

llm_manager.register_provider("claude", ProviderConfig(
    provider_type=ProviderType.CLAUDE,
    api_key="your-anthropic-api-key",
    api_base="https://api.anthropic.com",
    model_name="claude-3-5-sonnet-20241022"
))

# 绑定模块到特定Provider
llm_manager.register_module_provider("concept_extractor", "openai-fast")
llm_manager.register_module_provider("fact_checker", "default")
llm_manager.register_module_provider("wiki_merger", "claude")
```

### 3. 环境变量配置（推荐）

```bash
# .env 文件
# 主Provider（默认）
LLM_API_KEY=your-dashscope-api-key
LLM_MODEL_NAME=qwen-plus

# OpenAI配置
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL_NAME=gpt-4o

# Claude配置
CLAUDE_API_KEY=your-anthropic-api-key
CLAUDE_MODEL_NAME=claude-3-5-sonnet-20241022

# 模块级配置
CONCEPT_EXTRACTOR_PROVIDER=openai
CONCEPT_EXTRACTOR_MODEL=gpt-4o
```

---

## 功能特性

### ✅ 已实现功能

1. **多Provider支持**
   - 阿里百炼 (DashScope)
   - OpenAI 官方API
   - Claude (Anthropic)
   - 本地模型 (Ollama/LM Studio)

2. **模块级模型配置**
   - `concept_extractor`: 概念提取模块
   - `fact_checker`: 事实检查模块
   - `wiki_merger`: 维基合并模块

3. **向后兼容**
   - 原有 `.env` 配置完全兼容
   - 无需修改现有代码
   - 自动加载默认Provider

4. **统一接口**
   - `llm_manager.chat()`: 文本对话
   - `llm_manager.vision_chat()`: 视觉对话
   - `llm_manager.chat_with_messages()`: 多轮对话

5. **视觉模型支持**
   - 自动识别支持视觉的模型
   - 提供 `vision_chat()` 接口

### 🔧 API参考

#### ProviderConfig

```python
@dataclass
class ProviderConfig:
    provider_type: ProviderType      # Provider类型
    api_key: Optional[str]           # API密钥
    api_base: str                    # API地址
    model_name: str                  # 模型名称
    timeout: int = 60               # 超时时间
    max_tokens: Optional[int] = None # 最大token数
    temperature: float = 0.2         # 温度参数
```

#### LLMManager

```python
class LLMManager:
    def register_provider(self, name: str, config: ProviderConfig, set_default: bool = False)
    def register_module_provider(self, module_name: str, provider_name: str)
    def get_provider(self, module_name: Optional[str] = None) -> BaseLLMProvider
    def chat(self, prompt: str, system_prompt: Optional[str] = None,
             module: Optional[str] = None, temperature: Optional[float] = None, **kwargs) -> str
    def vision_chat(self, image_base64: str, prompt: str,
                    module: Optional[str] = None, **kwargs) -> str
    def list_providers(self) -> List[Dict[str, Any]]
    def list_module_bindings(self) -> Dict[str, str]
```

---

## 测试验证

### 语法检查

所有新增文件已通过Python语法检查：

```bash
python -m py_compile models/providers/base.py
python -m py_compile models/providers/dashscope.py
python -m py_compile models/providers/openai.py
python -m py_compile models/providers/claude.py
python -m py_compile models/providers/local.py
python -m py_compile models/llm_manager.py
python -m py_compile models/llm_core.py
python -m py_compile models/fact_checker.py
```

### 运行测试

```bash
python test_multi_provider.py
```

---

## 使用建议

### 模型选择策略

| 任务类型 | 推荐模型 | 理由 |
|---------|---------|------|
| 概念提取 | GPT-4o / Claude 3.5 Sonnet | 高质量理解与抽取 |
| 事实检查 | GPT-3.5-turbo / Qwen-plus | 快速便宜的裁判 |
| 笔记合并 | Claude 3.5 Sonnet | 擅长写作与整合 |
| 视觉分析 | GPT-4o / Claude 3.5 Sonnet | 优秀的视觉理解 |
| 本地开发 | Ollama llama3 / LM Studio | 节省成本、保护隐私 |

### 配置示例

**开发环境**（使用本地模型节省成本）：
```python
llm_manager.register_provider("local", LocalProvider.create_ollama_config("llama3"))
llm_manager.register_module_provider("concept_extractor", "local")
llm_manager.register_module_provider("fact_checker", "local")
```

**生产环境**（使用高性能模型）：
```python
llm_manager.register_provider("openai-gpt4", ProviderConfig(...))
llm_manager.register_provider("claude", ProviderConfig(...))
llm_manager.register_module_provider("concept_extractor", "openai-gpt4")
llm_manager.register_module_provider("fact_checker", "default")
llm_manager.register_module_provider("wiki_merger", "claude")
```

---

## 扩展指南

### 添加新的Provider

1. 在 `models/providers/` 创建新文件，如 `gemini.py`
2. 继承 `BaseLLMProvider` 基类
3. 实现 `chat()` 和 `supports_vision()` 方法
4. 在 `models/providers/__init__.py` 中导出
5. 在 `models/providers/__init__.py` 的 `PROVIDER_CLASSES` 中注册

```python
# models/providers/gemini.py
class GeminiProvider(BaseLLMProvider):
    def chat(self, messages, system_prompt=None, temperature=None, **kwargs):
        # 实现逻辑
        pass

    def supports_vision(self) -> bool:
        return "vision" in self.config.model_name.lower()
```

---

## 总结

本次修改实现了：

1. ✅ **多Provider支持** - 支持4种主流LLM服务
2. ✅ **模块级配置** - 不同模块使用不同模型
3. ✅ **向后兼容** - 原有配置完全可用
4. ✅ **统一接口** - LLMManager提供简洁API
5. ✅ **易于扩展** - 新的Provider只需继承基类

这是一个**breaking-change-free**的架构升级，现有代码无需任何修改即可继续运行，同时为未来支持更多模型服务预留了扩展接口。

---

**Issue链接**: （待补充）  
**解决版本**: v1.0+  
**状态**: ✅ 已实现
